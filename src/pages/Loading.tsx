import {FC} from 'react';

const LoadingPage: FC = () => (
  <div
    style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}
  >
    Loading...
  </div>
);

export default LoadingPage;
